import { Geometry, GLSettings, Material, Model } from "../../gl";
export declare function generateLayerModel(id: string, geometry: Geometry, material: Material, drawMode?: GLSettings.Model.DrawMode): Model;
