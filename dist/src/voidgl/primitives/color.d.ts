/**
 * This is the base information to have a functional Color within the GL system.
 * Each component represents a value of 0 - 1
 */
export declare type Color = {
    r: number;
    g: number;
    b: number;
    opacity: number;
};
